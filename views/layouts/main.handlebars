<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="">
    <meta name="description" content="Escritor de novela de crimen">
    <meta name="keywords"
        content="Impresión, vinil, backlite, Publicidad impresa, Medios impresos, Stoppers, espectaculares, Valla publicitarias, Material pop, Banners, Lonas, Viniles, Imprenta, Cenefa, Mupi, Posters, Danglers, Standees, Mantas, Faldones, Signs, Anuncios espectaculares, Anuncios impresos, Precio de espectaculares, Medios masivos">
    <link rel="shortcut icon" href="assets/images/logoVilpaLetras.jpg">
    <script src="https://kit.fontawesome.com/a077a0470e.js" crossorigin="anonymous"></script>
    <title>Alejandro Vilpa</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter&family=Josefin+Sans:ital,wght@1,600&family=Nunito+Sans:wght@200&family=Work+Sans:wght@300&display=swap"
        rel="stylesheet">

    <!-- Lato Font -->
    <link href='http://fonts.googleapis.com/css?family=Lato:300,400,700' rel='stylesheet' type='text/css'>

    <!-- Stylesheet -->
    <link href="/assets/css/gallery-materialize.min.css" rel="stylesheet">
    <!-- Stylesheet -->
    <link href="/assets/css/style.css" rel="stylesheet">
    <link href="/assets/css/materialize.min.css" rel="stylesheet">
    <link href="/assets/css/gallery-materialize.css" rel="stylesheet">


    <!-- Material Icons -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var elems = document.querySelectorAll('.carousel');
            var instances = M.Carousel.init(elems, options);
        });
    </script>

    <script>
        function submitEmailForm(form) {

            var obj = new XMLHttpRequest();
            obj.onreadystatechange = function () {
                if (obj.readyState == 4) {
                    if (obj.status == 200) {
                        var res = JSON.parse(obj.responseText);
                        alert(res.message);
                    }
                    else {
                        console.log("XMLHttp status " + obj.status + ": " + obj.statusText);
                    }
                }
            };
            obj.open("post", form.action, true);
            obj.setRequestHeader("Content-Type", "application/json"); // NOTICE: "application/json"
            obj.send(JSON.stringify({ name: form.name.value, email: form.email.value, message: form.message.value }));
            alert("¡Mensaje enviado con éxito!");
            $(".formInput").val(" ");
            return false;
        }
    </script>
    <script>
        function submitClubForm(form) {

            var obj = new XMLHttpRequest();
            obj.onreadystatechange = function () {
                if (obj.readyState == 4) {
                    if (obj.status == 200) {
                        var res = JSON.parse(obj.responseText);
                        alert(res.message);
                    }
                    else {
                        console.log("XMLHttp status " + obj.status + ": " + obj.statusText);
                    }
                }
            };
            obj.open("post", form.action, true);
            obj.setRequestHeader("Content-Type", "application/json"); // NOTICE: "application/json"
            obj.send(JSON.stringify({ name: form.nameClub.value, email: form.emailClub.value, country: form.countryClub.value }));
            alert("¡Bienvenido a la comunidad. Tu registro ha sido hecho con éxito!");
            $(".formInput").val(" ");
            return false;
        }
    </script>
    <script>
        function submitSmirnovForm(form) {

            var obj = new XMLHttpRequest();
            obj.onreadystatechange = function () {
                if (obj.readyState == 4) {
                    if (obj.status == 200) {
                        var res = JSON.parse(obj.responseText);
                        alert(res.message);
                    }
                    else {
                        console.log("XMLHttp status " + obj.status + ": " + obj.statusText);
                    }
                }
            };
            obj.open("post", form.action, true);
            obj.setRequestHeader("Content-Type", "application/json"); // NOTICE: "application/json"
            obj.send(JSON.stringify({ name: form.nameSmirnov.value, email: form.emailSmirnov.value, country: form.countrySmirnov.value, zipCode: form.zipSmirnov.value, phoneNumber: form.phoneSmirnov.value, dedica: form.dedicaSmirnov.value, address: form.addreSmirnov.value }));
            alert("¡Tu copia de La otra vida de Rebecca Smirnov, está en camino!");
            $(".formInput").val(" ");
            window.location.href = "https://www.alejandrovilpa.com/gracias";
            return false;
        }
    </script>
    <script>
        function submitInocentesForm(form) {

            var obj = new XMLHttpRequest();
            obj.onreadystatechange = function () {
                if (obj.readyState == 4) {
                    if (obj.status == 200) {
                        var res = JSON.parse(obj.responseText);
                        alert(res.message);
                    }
                    else {
                        console.log("XMLHttp status " + obj.status + ": " + obj.statusText);
                    }
                }
            };
            obj.open("post", form.action, true);
            obj.setRequestHeader("Content-Type", "application/json"); // NOTICE: "application/json"
            obj.send(JSON.stringify({ name: form.nameInocentes.value, email: form.emailInocentes.value, country: form.countryInocentes.value, zipCode: form.zipInocentes.value, phoneNumber: form.phoneInocentes.value, dedica: form.dedicaInocentes.value, address: form.addreInocentes.value }));
            alert("¡Tu copia de 'El aliento de los inocentes', está en camino!");
            $(".formInput").val(" ");
            window.location.href = "https://www.alejandrovilpa.com/gracias";
            return false;
        }
    </script>
</head>

<body>
    <div class="bodyPrincipal fonts">
        {{{body}}}
    </div>
    


    <!-- Core Javascript -->

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    <script>
        window.onload = function () {
            var button = document.getElementById('clickButton');
            button.click();
        };
    </script>
    <script src="/assets/js/imagesloaded.pkgd.min.js"></script>
    <script src="/assets/js/masonry.pkgd.min.js"></script>
    <script src="/assets/js/materialize.min.js"></script>
    <script src="/assets/js/color-thief.min.js"></script>
    <script src="/assets/js/galleryExpand.js"></script>
    <script src="/assets/js/init.js"></script>
    <script src="/assets/js/script.js"></script>
    <script>
        $(document).ready(function () {
            $('.slider').slider({ full_width: true });
        });
    </script>


</body>

</html>